<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('includes/head', { title: 'Post' }) %>
</head>
<body>
    <%- include('includes/header', { currentUrl: '/Post' }) %>
    <div class="container text-center">
        <h1>Add a new post</h1>
        <form action="/Post" method="POST">
            <div class="form-control">
                <label for="category">Select Category</label>
                <select id="category" name="category">
                    <% for (const category of categories)  { %>
                        <option value="<%= category.id %>"><%= category.name %></option>
                    <% } %>
                </select>
            </div>
            <div class="form-control">
                <label for="title">Title</label>
                <input type="text" id="title" name="title" required>
            </div>
            <div class="form-control">
                <label for="summary">Summary</label>
                <input type="text" id="summary" name="summary" required maxlength="255">
            </div>
            <div class="form-control">
                <label for="content">Post Content</label>
                <textarea id="content" name="content" required rows="5"></textarea>
            </div>
            <div class="form-control">
                <label for="filename" class="form-label">Put File</label>
                <input class="form-control form-control-lg" id="filename" name="filename" type="file">
            </div>
            <button class="btn btn-light">Add Post</button>
        </form>
    </div>

    <style>

        h1 {
            text-align: center;
        }
    
        label {
            font-weight: bold;
            display: block;
            margin-bottom: 0.25rem;
        }

        input,
        textarea,
        select {
            font: inherit;
            padding: 0.5rem;
            border-radius: 4px;
            border: 1px solid rgb(204, 204, 204);
        }
    
        input:focus,
        textarea:focus {
            background-color: rgb(237, 228, 255);
        }
    
        input,
        textarea {
            width: 100%;
        }
    
        .form-control {
            margin: 0.5rem 0;
        }
    </style>
</body>
</html>